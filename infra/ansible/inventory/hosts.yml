all:
  children:
    frontend:
      hosts:
        frontend_server:
          ansible_host: 13.60.204.224
          ansible_port: 22

    backend:
      hosts:
        backend_server:
          # Use your backend private IP
          ansible_host: 51.20.131.10
          ansible_port: 22
          # FIXED: Clean, robust routing using modern SSH extra arguments
          ansible_ssh_extra_args: '-o ProxyCommand="ssh -W %h:%p -i ~/.ssh/task-app.pem -o StrictHostKeyChecking=no ubuntu@13.60.204.224"'

    database:
      hosts: {}

  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/task-app.pem
